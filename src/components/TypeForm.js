import React from "react";
import { Widget } from "@typeform/embed-react";

const TypeForm = () => {
  const widgetContainerStyle = {
    height: "100vh",
    margin: "20px auto",
  };
  return (
    <>
      <Widget id="wEoh2oJ7" style={widgetContainerStyle} />
    </>
  );
};

export default TypeForm;
